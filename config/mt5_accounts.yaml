# MT5 Account Configuration
# 1. Set VALETAX_PASSWORD as environment variable (recommended)
# 2. Or replace ${VALETAX_PASSWORD} with your actual password
# 3. To find your server: open MT5 → File → Login → copy "Server" field
accounts:
  - name: "MT5 Demo"
    broker: mt5
    login: 372044706        # Your MT5 demo login
    server: "ValetaxIntl_Live-2"  # Your MT5 server
    password: "${VALETAX_PASSWORD}"  # Set VALETAX_PASSWORD env var
    paper: false  # false = demo/real money, true = paper simulation


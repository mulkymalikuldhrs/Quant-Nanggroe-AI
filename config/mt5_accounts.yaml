# MT5 Accounts — Dikonfigurasi untuk Valetax Demo
# Password DIISI oleh user sebelum live. File ini .gitignore'd.
# Env var alternatif: $VALETAX_PASSWORD (via os.path.expandvars)
accounts:
  - name: valetax_demo
    broker: mt5
    login: 372044706
    password: "${VALETAX_PASSWORD}"  # ganti dengan password asli, atau set env VAR
    server: "ValetaxIntl_Live-2"
    paper: false  # demo = real MT5, tapi uang virtual


# QNA 7-Agent Council Report — Upgrade Path to Autonomous Quant HF (2026-07-16)

Generated by @dhaherautobot orchestrating 7 isolated profiles (autobot/clawbot/fangbot/
hackerbot/devbot/traderbot/researchbot). Each profile ran as a leaf agent, reported ONLY
to orchestrator, relayed here as @mention. No inter-profile communication (per user SOP).

## State at council time
- QNA v4.5.10 (post C6): column-case root-cause FIXED → **75 of 106 strategies now fire
  >10 non-zero 1h signals** on real BTC/ETH/SOL yfinance data.
- Execution engine: 1818/1819 tests pass (1 pre-existing flake). Real trades confirmed
  (KalmanFilter walk-forward: 13059 OOS trades, under_sampled=False).
- Maturity doc amended: signal-density B, alpha PENDING (now measurable).

## Profile findings (verbatim relay)
- **@autobot**: Isolated lanes + labeled-queue routing; sequential merge (research→dev→
  hacker→trader). Next concrete step: `BaseStrategy.fit()` + real walk-forward, measure
  live signal count = LIVE gate.
- **@clawbot**: "Production-grade" claim unproven — 1818/1819 are MOCK unit tests. Real
  proof = MT5 DEMO end-to-end, 7d, assert ≥1 real order + kill-switch trips on feed loss.
- **@fangbot**: ⚠️ STALE PREMISE — diagnosed AND-gate SMC as root cause, but that was
  pre-C6 (repo was signal-dead then). Post-C6, 75/106 fire. Fix only relevant to 31
  scaffolds needing L2/on-chain, NOT the 75 living strategies.
- **@hackerbot**: Kill-switch is FAIL-OPEN (common-mode: same process tree as bridge).
  Hardening = independent watchdog outside bridge tree, hard-close via MT5 API on
  heartbeat/P&L staleness. Required before any live capital.
- **@devbot**: No portfolio/aggregation layer (106 silos). First build = PortfolioManager
  (equal-weight + max-pos cap + vol cap). `fit()` = YAGNI now.
- **@traderbot**: 0 trade = unproven edge. SHADOW mode (0 real capital, live signals).
  Go-live gate: expectancy>0, t-stat≥1.96, PF>1.2, ≥30 closed trades.
- **@researchbot**: Add L2 Order-Flow Imbalance (OFI) + cross-asset regime gate
  (DXY/rates/gold). Ref: Cont, Kukanov & Stoikov (2014).

## Orchestrator synthesis (Ponytail)
1. **Edge measurement is the ONLY gate** — firing ≠ edge. Next action: walk-forward
   expectancy on 75 living strategies through v4.5.9 significance gate.
2. **fangbot's AND-gate fix deferred** — premature; 75 already fire post-C6. Revisit only
   for the 31 scaffolds if L2 data is acquired.
3. **hackerbot watchdog = pre-live blocker** — fail-open kill-switch is a real risk.
4. **PortfolioManager = post-edge** — YAGNI until ≥10 strategies clear the expectancy gate.
5. **No live capital** until shadow expectancy validated (traderbot gate).

## Action items
- [ ] Measure OOS expectancy for top-15 firing strategies (shadow, no capital) — IN PROGRESS
- [ ] Independent kill-switch watchdog (hackerbot hardening)
- [ ] PortfolioManager stub (devbot) — after edge validated
- [ ] L2 OFI signal source eval (researchbot) — optional, post-edge
